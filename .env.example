# ─── Produção (MySQL) — schema.prisma usa provider = "mysql" ───
# Copie para .env:  cp .env.example .env
DATABASE_URL="mysql://user:password@host:3306/elochurch"

# ─── Desenvolvimento local (SQLite) ───
# npm run db:use:sqlite  (copia schema.sqlite.prisma → schema.prisma)
# DATABASE_URL="file:./dev.db"

# URL pública canônica (domínio) — QR da carteirinha, links /membro/[codigo]
# OBRIGATÓRIO em produção: use o domínio, NUNCA IP
APP_URL="https://elechurch.adcec.com.br"
NEXT_PUBLIC_APP_URL="https://elechurch.adcec.com.br"
NEXTAUTH_URL="https://elechurch.adcec.com.br"

NEXTAUTH_SECRET="dev-secret-change-in-production"

# Super admin (seed)
SUPER_ADMIN_EMAIL="admin@elochurch.com"
SUPER_ADMIN_PASSWORD="Admin@123"
SUPER_ADMIN_NOME="Super Administrador"

# Recriar todos os dados de demo: DEV_SEED_RESET=1 npm run db:seed
